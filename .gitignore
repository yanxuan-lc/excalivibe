.DS_Store
*.log
node_modules/
.venv/
__pycache__/

# zread generated wiki (derivable build artifact, regenerate with `zread generate`)
.zread/

# 本仓库开发期用的 agent 本地配置与集成目录。仅供在本仓库内开发时使用，不应随 git-源
# marketplace 分发给消费者 —— 否则会被 clone 进消费者机器、注入技能并触发未受信项目警告。
#
# 三条全部用 / 锚定到仓库根，这不是风格问题：根目录的 `.agents/` 装的是 **Codex 的
# marketplace 清单**（`.agents/plugins/marketplace.json`，由 make build 生成且必须提交），
# 与这里要忽略的本地配置目录同名相近。不锚定的话，任意层级的同名目录都会被吞掉，而构建
# 产物被静默漏提交时 verify-build 在本地依然是绿的 —— 消费者 clone 到的却是坏的清单。
/.codex/
/.claude/
/.agent/
