- **<YYYY-MM-DD>** · `draft` — <who or what raised it, and off the back of what>

<!--
One line per event, appended, never edited. The line shape:

  - **<date>** · `<state>`[ · `<change ids>`][ — <one line>]

Date only, no time: order comes from line position, so a later line is a later event.

The third segment appears only on `done`, and lists every change the item finally landed in — it is
the only link between a requirement and its implementation.

A backward transition carries a reason; a forward one need not.

The last line of this file is the truth about the item's state. The `status` in brief.md mirrors it.

Delete this comment once the file has real history in it.
-->
