id: genai.perf
kind: verify
executor: perf-runner

inputs:
  - name: code
    from: genai.implement

outputs:
  - name: report
    kind: file
    locator: openspec/changes/{{vars.change}}/genai/perf-report.md
    signature: checksum

gates:
  - rule_id: still_valid
    checker:
      type: signature_match
      state_key: checked_code
      against: code
    on_result:
      matched:
        verdict: pass
      mismatched:
        verdict: reject
        message: the code has changed since the performance check ran, so the earlier conclusion no longer holds
  - rule_id: outputs
    checker:
      type: outputs_present
    on_result:
      satisfied:
        verdict: pass
      missing:
        verdict: reject
        message: the performance report is not on disk
  - rule_id: outcome
    checker:
      type: report
      field: outcome
    on_result:
      completed:
        verdict: pass
        state:
          checked_code: "{{inputs.code}}"
      partial:
        verdict: reject
        message: the performance check completed only partly - resolve the open items in the report
      failed:
        verdict: reject
        message: the performance check did not pass - work through the report
      blocked:
        verdict: delegate
        message: the tooling or environment is not available - not something the implementation can fix
